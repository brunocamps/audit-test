<%= content_for :title, t("shared.scaffold.edit.title", model: "Company Area") %>

<div class="container mx-auto px-4 my-8">
  <div class="flex items-center justify-between mb-4">
    <h1 class="h3">
      <%= link_to "Company Areas", company_areas_path, class: "text-black" %>
      <span class="text-gray-400 font-light mx-2">\</span>
      <%= t("shared.scaffold.edit.title", model: "Company Area") %>
    </h1>
    <%= button_to t("delete"), @company_area, method: :delete, class: "btn btn-white", form: { data: { turbo_confirm: t("are_you_sure") } } %>
  </div>

  <div class="p-8 bg-white rounded shadow">
    <%= render "form", company_area: @company_area %>
  </div>
</div>
